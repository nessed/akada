/**
 * Where a user can actually reach a person.
 *
 * Named in one place because the privacy policy, the terms and the Settings
 * row all have to agree, and a support address that silently bounces is
 * worse than none at all.
 */
export const CONTACT_EMAIL = 'ali.abid44444@gmail.com';

/** The name published as the data controller. */
export const CONTROLLER_NAME = 'Ali Abid';
